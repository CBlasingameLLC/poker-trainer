{
  "name": "poker-trainer",
  "version": "0.1.0",
  "description": "Texas Hold'em No-Limit trainer PWA — targeted practice and personal statistics. Companion to Blackjack Pro.",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "verify": "node scripts/verify-hand-eval.js && node scripts/verify-ranges.js && node scripts/verify-postflop.js && node scripts/verify-progress.js",
    "cap:sync": "cap sync",
    "cap:ios": "cap open ios",
    "cap:android": "cap open android",
    "build:cap": "vite build && npx cap sync"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  }
}
